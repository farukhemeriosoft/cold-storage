{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "frontend:dev": "cd resources/frontend && npm run dev",
        "frontend:build": "cd resources/frontend && npm run build",
        "dev:all": "concurrently \"npm run dev\" \"npm run frontend:dev\"",
        "build:all": "npm run build && npm run frontend:build"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.2.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.4"
    }
}
